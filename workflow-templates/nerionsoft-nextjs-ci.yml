name: Next.js CI

on:
  push:
    branches: [$default-branch]
  pull_request:
    branches: [$default-branch]

jobs:
  ci:
    uses: NerionSoft/nerionsoft-cicd/.github/workflows/reusable-nextjs-ci.yml@main
    # with:
    #   node-version: "24"
    #   run-lint: true
    #   run-format: true
    #   run-typecheck: true
    #   run-test: true
    #   run-build: true
    #   run-e2e: true
    #   run-hadolint: false
    #   dockerfiles: "Dockerfile"   # whitespace/newline-separated paths or globs
    #   run-sonar: false
    #   sonar-project-key: ${{ vars.SONAR_PROJECT_KEY }}
    #   sonar-project-name: ${{ vars.SONAR_PROJECT_NAME }}
    # secrets:
    #   CI_DATABASE_URL: ${{ secrets.CI_DATABASE_URL }}
    #   CI_BETTER_AUTH_SECRET: ${{ secrets.CI_BETTER_AUTH_SECRET }}
    #   PAYLOAD_SECRET: ${{ secrets.PAYLOAD_SECRET }}
    #   SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    #   SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
    #   SONAR_ROOT_CERT: ${{ secrets.SONAR_ROOT_CERT }}
