name: Python CI

on:
  push:
    branches: [$default-branch]
  pull_request:
    branches: [$default-branch]

jobs:
  ci:
    uses: NerionSoft/nerionsoft-cicd/.github/workflows/reusable-python-ci.yml@main
    # with:
    #   python-version: "3.13"
    #   run-ruff: true
    #   run-mypy: true
    #   run-test: true
    #   run-sonar: false
    #   sonar-project-key: ""
    #   sonar-project-name: ""
    # secrets:
    #   SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    #   SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
    #   SONAR_ROOT_CERT: ${{ secrets.SONAR_ROOT_CERT }}
